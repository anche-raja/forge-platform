[pytest]
# The suite is written as plain functions — there is not one test class in it.
# Left at the default, pytest tries to collect every class named Test* it sees
# while importing the package (TestGenAgent, TestReviewer, TestGenSettings ...)
# and warns that it cannot. Those are domain names, not test classes.
python_classes =
